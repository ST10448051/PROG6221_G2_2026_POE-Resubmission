<Window x:Class="CyberSecurityAwarenessBot.GUI.Views.TaskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Task Manager"
        Height="650"
        Width="850"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto" MinHeight="194"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock Text="Task Title" Margin="0,0,0,1"/>

        <TextBox Name="TitleBox"
                 Grid.Row="1"
                 Margin="0,5,0,15"/>

        <TextBlock Grid.Row="2"
                   Text="Description" Margin="0,0,0,91" Grid.RowSpan="2"/>

        <TextBox Name="DescriptionBox"
                 Grid.Row="3"
                 AcceptsReturn="True"
                 Margin="0,29,0,53"/>

        <Button Name="AddTaskButton"
        Content="Add Task"
        Width="147"
        HorizontalAlignment="Left"
        Margin="0,153,0,10" Grid.Row="3"/>

        <DataGrid Name="TaskGrid"
                  Grid.Row="4"
                  AutoGenerateColumns="True"/>

        <Button Name="DeleteTaskButton"
        Content="Delete Selected"
        Width="147"
        Margin="170,153,0,10"
        HorizontalAlignment="Left"
        Grid.Row="3"/>

    </Grid>

</Window>